import 'package:flutter/material.dart';

class ContainerTwo extends StatelessWidget {
  ContainerTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('error'),
      ),
    );
  }
}
